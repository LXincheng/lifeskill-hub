spring:
  datasource:
    url: jdbc:h2:mem:lifeskill;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
  flyway:
    enabled: false
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: none
  ai:
    deepseek:
      api-key: test-key
