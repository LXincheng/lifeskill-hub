package dev.lifeskill.conversation.application.model;

public record ModelSkillDraftProposal(
        String title,
        String objective,
        String dayOfWeek,
        String time,
        String timezone) {
}
